export type Inputs = {
    fullname: string;
    username: string;
    email: string;
    password: string;
  };
export type FormRegisterProps = {
    loading: boolean;
    handleLoading: Function;
  }